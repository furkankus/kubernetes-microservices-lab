trigger:
  - main

pool:
  vmImage: ubuntu-latest

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'dockerhub'
    repository: 'furkankus/user-service'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: |
      latest
